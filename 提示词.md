# LeetCode Hot 100 · C++ 学习库 — 使用指南与高效刷题提示词

> 适合想系统刷力扣热题 100 的初学者 / 进阶者。这份文档告诉你是**怎么用这个项目、怎么学、怎么避坑**。

---

## 📦 项目里有什么

```
pi-test/
├── src/                  # 100 道题的 C++ 源码（每类一个文件）
│   ├── 07_linked_list.cpp    # 链表 14 题
│   ├── 15_dp.cpp               # 一维 DP 10 题
│   ├── ...                   # 共 17 个文件
│   └── main.cpp              # 测试驱动器
├── include/
│   ├── common.h              # ListNode / TreeNode 定义 + 工具函数
│   └── test.h                # 极简测试框架（零依赖 ~120 行）
├── site/                     # 可视化讲解网站（可离线双击打开）
│   ├── index.html            # 首页：100 题分类 + 搜索 + 进度
│   ├── problems/q1.html …    # 每题一页：图解 + 代码 + 测试
│   └── assets/               # 样式 + 脚本
├── tools/                    # 生成工具的 Python 脚本
│   ├── gen_site.py           # 从源码注释生成网站
│   ├── gen_animations.py     # 生成算法动图
│   └── fetch_descriptions.py # 抓取力扣题目描述
├── data/                     # 题目描述数据
├── build.sh                  # 一键编译 + 跑测试
├── build_site.bat / .sh      # 重新生成网站
└── CMakeLists.txt            # CMake 构建配置
```

### ⚡ 三条最快上手路径

| 你想干什么 | 怎么做 |
|---|---|
| **看讲解、看图** | 浏览器打开 `site/index.html`，双击即可，不联网 |
| **写代码 + 验证** | 改 `src/xx.cpp` 里的 Solution → `bash build.sh` → 看结果 |
| **新增一道题** | 在 `src/` 新建文件（命名如 `18_xxx.cpp`），加 namespace + TEST，自动被扫描到 |

---

## 🎯 高效刷题方法（三轮刷法）

### 第一轮：只看注释，不看书

打开某个分类文件（比如 `src/07_linked_list.cpp`），**先读文件头部的「通用心法」**，然后再读每道题的注释块。

**不要直接看实现代码！** 盖住 `Solution` 类的代码部分，自己口头说出：
- 这题的核心思路是什么？
- 时间 / 空间复杂度是多少？
- 这道题最容易在哪里出错？

答不上来再看注释。**同类题一个模板，比单刷十道更有用。**

#### 💡 分类模板速查

每个源文件头部有一段分类总结，类似这样：

```cpp
//  【链表通用心心】
//  1) 哨兵结点 dummy：凡是「头结点可能被删/被换」的题，一律从 dummy 出发
//  2) 快慢指针三件套：找中点、找倒数第 k、判环
//  3) 反转是基本功：prev / cur / nxt 三个指针，先存 nxt 再改链
```

这些模板是你以后面试时能快速回忆的关键记忆点。

---

### 第二轮：自己手写实现，用测试验证

把某道题的 `Solution` 类删掉（或先在别的地方重写），然后重新写一遍，回到原文件跑测试：

```bash
# 只跑 Q206 那组测试
./build/hot100 q206_

# 只跑链表全部 14 题
./build/hot100 linked_list

# 同时命中 dp 和 dp_multidim
./build/hot100 dp
```

**只有用例全绿才算做对**——这里没有「感觉对了」。测试框架会打印失败时的 actual vs expected。

#### 🧪 常用测试过滤命令

```bash
./build/hot100                # 跑全部 99 个用例
./build/hot100 hash           # 跑哈希组（Q1, Q49, Q128）
./build/hot100 binary_tree    # 跑二叉树组（15 题）
./build/hot100 q42_           # 跑 Q42 接雨水
./build/hot100 backtracking   # 跑回溯组（8 题）
./build/hot100 dp_multidim    # 跑多维 DP（5 题）
```

---

### 第三轮：按套路横向串题

看完索引表最后一列的「核心套路」，**同一套路连着做几道题**，效果翻倍：

| 套路 | 连着做这几题 |
|---|---|
| 对撞双指针 | Q11 → Q15 → Q42 |
| 滑动窗口 | Q3 → Q438 → Q76 → Q560（对比：有负数只能前缀和） |
| 单调栈 | Q739 → Q84 → Q42（单调栈解法） |
| 快慢指针 / 改链表指针 | Q206 → Q141 → Q142 → Q19 → Q160 → Q234 |
| 前缀和 + 哈希 | Q560 → Q437（树版）→ Q1（同一张哈希表的三种用法） |
| 二分找边界 | Q35 → Q34 → Q74 → Q33 → Q153 |
| BFS 分层 | Q102 → Q199 → Q994 → Q207 |
| 回溯三行模板 | Q46 → Q78 → Q39 → Q22 → Q131 → Q51 |
| 完全背包 vs 0-1 背包 | Q322/Q279（正序）↔ Q416（逆序） |
| 树形 DP | Q543 → Q124 → Q236 |
| 一维滚动降维 | Q62 → Q64 → Q1143 → Q72 |
| 对顶堆 / 第 K 大 | Q215 → Q347 → Q295 |

---

## 🔥 必须背下来的 12 个坑

以下每条都对应至少一道容易写错的题，建议截图贴墙上：

1. **`mid = lo + (hi - lo) / 2`** —— 防 `(lo+hi)` 溢出（所有二分题）
2. **区间语义要统一**：闭区间 `[lo, hi]` 配 `hi = mid - 1`；半开区间 `[lo, hi)` 配 `hi = mid`。混用 = 死循环
3. **一维 0-1 背包内层必须逆序**；完全背包正序（Q416 vs Q322）。正序写错会让同一个数用两次
4. **「以 i 结尾」型 DP，答案是 `max(dp[i])` 不是 `dp[n-1]`**（Q300、Q152、Q53）
5. **乘积最大子数组初值必须是 `nums[0]`**，写 0 会在全负数组上返回 0（Q152）
6. **Q437 是「任意起点向下」的路径**，不是根到叶。要用前缀和 + 哈希，且 `presum[0]=1` 要先塞进表
7. **验证 BST（Q98）要比上下界**，只比较父子结点会被 `[5,4,6,null,null,3,7]` 打穿
8. **链表改指针前先存 `next`，且用 dummy 头结点**统一处理（Q19、Q24、Q25）
9. **荷兰国旗遇到 2 时与 `p2` 交换后 `i` 不能自增**（Q75）—— 换回来的值还没看过
10. **Q287 的输入必须满足「长度 n+1、值域 [1,n]」**，否则快指针真的会越界（段错误）
11. **`unordered_map` 的 `operator[]` 会插入默认值**，判存在用 `count()` / `find()`（Q1、Q560、Q437）
12. **`std::priority_queue` 默认是大顶堆**，要小顶堆得写满三个模板参数（Q23、Q215、Q347）

---

## 🖥️ 可视化网站使用技巧

打开 `site/index.html` 后你可以：

### 1. 每题一页的详细讲解

点击任意题目进入详情页，每页包含：
- **原始题目描述**（含示例、约束，不用去力扣翻题了）
- **核心套路一句话总结**
- **带注释的完整代码**
- **结构图解**（链表画指针走向，树画递归树，矩阵画遍历轨迹）
- **算法动图**（78 题支持 ▶ 播放，能看到指针怎么走、窗口怎么滑、树怎么遍历、DP 表怎么填）

### 2. 动图控制面板

点击播放后底部会出现控制条：
- ▶ **播放 / ⏸ 暂停**
- **单步前进**（逐步观察）
- **速度调节**：0.5× → 0.75× → 1× → 1.5× → 2×

建议复杂题先用 **0.5× 或 0.75×** 慢慢看指针移动过程。

### 3. 搜索与筛选

首页顶部可以：
- 按关键字搜索题号或分类
- 按难度筛选（Easy / Medium / Hard）
- 查看自己的学习进度

### 4. 全新生成

如果需要更新内容：
```bash
# Windows 双击
build_site.bat

# 或 bash（Git Bash / WSL）
bash build_site.sh
```

这会重新从 `src/*.cpp` 提取注释生成新的页面。

---

## 🛠️ VS Code / Cursor 开发指南

仓库已配好 `.vscode/` 目录，开箱即用：

| 操作 | 快捷键 / 方式 |
|---|---|
| 打开工程 | `File → Open Folder` 选本目录 |
| 编译 + 跑测试 | `Ctrl+Shift+B` |
| 打断点调试 | 按 `F5`，弹出框里输过滤词（如 `q76`） |
| 代码跳转 | 装 C/C++ 扩展后即可 `Ctrl+点击` |

> ⚠️ **注意**：确保 `C:/mingw64/bin` 排在 PATH 最前面，否则会加载错 DLL 导致 gdb 启动失败（报错 `0xc0000139`）。`launch.json` 已做了环境变量覆盖处理。

---

## 📝 代码规范速览

如果你打算往里面加新题，记住三点：

1. **每个 `namespace lcNNNN` 里只有一个 `class Solution`**
   函数签名完全对齐力扣，可以直接复制粘贴到答题区。

2. **注释块固定格式**：
   ```cpp
   /*
    * QNNN. 题目名  Diffculty
    * https://leetcode.cn/problems/xxx/
    *
    * 思路：……
    * 复杂度：时间 O(x)，空间 O(y)
    * 易错：……
    * 变体：……
    */
   ```
   `gen_site.py` 会自动解析这些字段生成网页。

3. **至少覆盖三类测试**：官方示例 + 边界情况（空 / 单元素 / 全相同）+ 反直觉 case。

---

## 📊 学习路线推荐

| 阶段 | 顺序 | 目标 |
|---|---|---|
| **入门期**（2-3 周） | 技巧(17) → 哈希(01) → 双指针(02) → 栈(12) → 简单数组(05) | 熟悉语言语法，建立基本信心 |
| **基础期**（3-4 周） | 链表(07) → 二叉树(08) → 二分查找(11) → 滑动窗口(03) → 前缀和(04) | 掌握数据结构 + 经典算法模式 |
| **进阶期**（4-5 周） | 堆(13) → 贪心(14) → 一维 DP(15) → 多维 DP(16) → 回溯(10) | 攻克动态规划和搜索两大难关 |
| **冲刺期**（2-3 周） | 图论(09) → 补漏 Hard 题 → 横向串题复习 | 覆盖全部题型，查漏补缺 |

**每周建议**：每天 1-2 题，写完立刻跑测试。宁可少刷，也要真正搞懂一类题的一个模板。

---

## ❓ FAQ

**Q：我电脑没装 g++ 怎么办？**
A：安装 MinGW-w64（或 MSYS2），然后把 `bin/` 加入 PATH。或者直接用 VS Code 内置的 CMake Tools。

**Q：网站能不能在手机上用？**
A：能！纯 HTML + CSS + JS，手机浏览器打开就行，但动图在小屏上可能有点挤。

**Q：我能参考这里的代码写在别的题库上吗？**
A：完全可以。每道题的 `Solution` 类可以直接复制到力扣提交框。唯一需要注意的就是那 12 处 `const&` 改动——力扣不需要它们但不冲突。

**Q：怎么给自己加一道力扣上遇到的好题？**
A：
1. 在 `src/` 新建 `18_extra.cpp`（序号递增）
2. 复制某个现有文件的开头作为模板（include + namespace 约定）
3. 写你的 `Solution` + `TEST` 用例
4. 运行 `bash build.sh` 确认通过
5. （可选）用 `gen_site.py` 生成本题的网站页面

**Q：测试全过不代表答案正确？**
A：没错。测试用例覆盖了你设计的那些 edge case，但如果题目有特殊限制你没考虑到，仍然可能 WA。不过对于练习来说，99 个用例 + 自己多想 2-3 个边界，已经能解决 95% 的问题。

---

> **最后提醒**：这个项目的精髓不在于 100 道题本身，而在于**同一套路的多次重复训练**。同一种模板出现 N 次的时候，你会突然发现："哦原来这么多题是一个东西！" 那就说明你真的学会了。

祝刷题顺利 🚀
